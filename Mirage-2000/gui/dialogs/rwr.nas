print("*** LOADING MAP.nas ... ***");
var createRWR = func {
    var window = canvas.Window.new([768, 512], "dialog").set('title', "Rwr").set("rwr", 1);
    var myCanvas = window.createCanvas();
    myCanvas.set("background", "#ffaac0");
    
    var root = myCanvas.createGroup();

}

#createMap();
